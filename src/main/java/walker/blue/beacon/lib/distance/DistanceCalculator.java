package walker.blue.beacon.lib.distance;

import walker.blue.beacon.lib.beacon.Beacon;

/**
 * Placeharder distance calculation class
 */
public class DistanceCalculator {

    /**
     * Calculate distance between the user and the given Beacon
     *
     * @param beacon Beacon whose distace will be calculated
     * @return diastance between the given beacon and the user
     */
    public static double calculateDistance(final Beacon beacon) {
        return -1.0f;
    }
}